---
// Layout and Components
import Layout from "../layouts/Layout.astro";
import Header from "../components/structure/Header.astro";
import SectionContainer from "../components/structure/SectionContainer.astro";
import Timeline from "../components/elements/Timeline.astro";
import TrayArrowDown from "phosphor-astro/TrayArrowDown.astro";
import TenetCard from "../components/elements/TenetCard.astro";
import AtomSymbol from "../components/elements/AtomSymbol.astro";
import PersonalSection from "../components/sections/about/PersonalSection.astro";
import ToolsSection from "../components/sections/about/ToolsSection.astro";
import LinkButton from "../components/elements/LinkButton.astro";

// Data
import { timelineItems } from "../data/timeline";
import { tenets } from "../data/tenets";

// Assets
import { Image } from "astro:assets";
import karina from "../assets/brand/karina-profile.png";
import framebg from "../assets/avatars/avatar-4.png";
---

<Layout
  pageTitle="About - Karina Tovar"
  pageDescription="Pixel Philosopher Cautious Technologist"
>
  <div class="layout--two-columns">
    <div class="layout--two-columns__left glued">
      {/* Hero Section */}
      <Header type="default" size="small" title="Hello Stranger" />
      <figure
        class="profile-frame"
        style={`background-image: url(${framebg.src})`}
      >
        <Image
          src={karina}
          alt="Karina Tovar"
          class="profile-frame--image"
          width={600}
          height={600}
          id="themeImage"
        />
      </figure>
    </div>

    <div class="layout--two-columns__right relative">
      {/* Introduction */}
      <SectionContainer id="introduction">
        <h2>My Background</h2>
        <p class="copy-large mt-6">
          I am an artist, turned product designer, who likes to code and make
          things
        </p>
        <p>
          I have a background in the Fine Arts, with a MFA from the
          Massachusetts College of Art & Design. There I was part of the Studio
          for Interrelated Media where I made artwork focused on performative
          technology. That just means that I obsessed over the intersection of
          technology and human happiness. I worked on a lot of performance art
          pieces that lived outside of the museum so I could create room for joy
          in the mundane.
        </p>
      </SectionContainer>

      {/* Tenets Section */}
      <SectionContainer id="tenets" spacing="lg">
        <AtomSymbol include_classes="w-20 h-20 mb-10" />
        <h2>My Design Tenets</h2>
        <div class="grid grid-cols-1 gap-8 mt-8 w-full">
          <TenetCard items={tenets} />
        </div>
      </SectionContainer>

      {/* Experience Section */}
      <SectionContainer id="experience" spacing="lg">
        <AtomSymbol include_classes="w-20 h-20 mb-10" />
        <div
          class="flex justify-between items-baseline flex-wrap gap-4 mb-10 group"
        >
          <h2>A Decade of Experience</h2>
          <a href="#" class="resume-link">
            <TrayArrowDown class="w-6 h-6" />
            <span>Download Resume</span>
          </a>
        </div>
        <Timeline items={timelineItems} />
        <LinkButton
          href="https://www.youtube.com/"
          label="Get My Resume"
          hover_label="Download PDF"
          include_classes="mt-10 ml-5"
          html_data={{ target: "_blank" }}
        />
      </SectionContainer>

      {/* Tools Section */}
      <SectionContainer id="tools" spacing="lg">
        <ToolsSection />
      </SectionContainer>

      {/* Personal Section */}
      <SectionContainer id="personal" spacing="lg">
        <PersonalSection />
      </SectionContainer>
    </div>
  </div>
</Layout>

<style>
  .profile-frame {
    @apply flex justify-center items-center w-fit pt-20 rounded-t-full bg-theme-highlight-orange overflow-hidden bg-cover bg-center;
  }

  .profile-frame--image {
    @apply mx-auto xl:mx-0 xl:max-w-[50vh] mix-blend-overlay translate-x-8;
  }

  .resume-link {
    @apply flex items-center gap-2 h-5 w-fit text-sm transition-all duration-300;
    span {
      @apply underline-offset-4;
    }
    &:hover span {
      @apply underline;
    }
  }
</style>
