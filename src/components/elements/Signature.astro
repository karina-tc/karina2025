---
interface Props {
  text?: string;
  includeClasses?: string;
}

const { 
  text = "Life is good!", 
  includeClasses = "" 
} = Astro.props;
---

<div class={`text-4xl opacity-50 py-20 ${includeClasses}`}>
  <span>- {text}</span>
</div> 