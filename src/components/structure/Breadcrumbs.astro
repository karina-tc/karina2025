---
interface Crumb {
  text: string;
  href: string;
  showArrow?: boolean;
}

interface Props {
  crumbs: Crumb[];
}

const { crumbs } = Astro.props;
---

<nav class="breadcrumbs">
  {
    crumbs.map((crumb) => (
      <a href={crumb.href} class="crumb group">
        {crumb.showArrow && <span class="arrow">←</span>}
        <span class="label">{crumb.text}</span>
      </a>
    ))
  }
</nav>

<style>
  .breadcrumbs {
    @apply flex items-center gap-2 py-10;
  }

  .crumb {
    @apply flex items-center gap-2 transition-all;
  }

  .label {
    @apply underline-offset-4 font-semibold group-hover:underline;
  }

  .arrow {
    @apply w-10 h-10 flex items-center justify-center rounded-full transition-all duration-300;
    @apply bg-stone-100 shadow-inner shadow-stone-300;
    @apply group-hover:bg-stone-200;
  }
</style>
