---
import Template from "./Template.astro";

interface Props {
  title: string;
  description?: string;
  date: string;
}

const { title, description, date } = Astro.props;
---

<Template
  title={title}
  description={description}
  date={date}
  source="markdown"
>
  <slot />
</Template>
