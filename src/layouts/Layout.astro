---
// Global Styles
import "../styles/global.css";

// Components
import CommonHead from "../components/structure/CommonHead.astro";
import Preloader from "../components/structure/Preloader.astro";
import StickyNav from "../components/elements/StickyNav.astro";
import Footer from "../components/structure/Footer.astro";

// Theme
import texture from "../assets/backgrounds/2-texture.png";

interface Props {
  pageTitle?: string;
  pageDescription?: string;
}

const {
  pageTitle = "Karina Tovar",
  pageDescription = "Pixel Philosopher Cautious Technologist",
} = Astro.props;

// Get the current path
const currentPath = Astro.url.pathname;
const isHomePage = currentPath === "/";
---

<!doctype html>
<html lang="en" data-theme="dark">
  <CommonHead {pageTitle} {pageDescription} />
  <body>
    <div class="root bg-contain bg-repeat">
      {isHomePage && <Preloader />}
      <StickyNav />
      <main class="main">
        <div class="content">
          <slot />
        </div>
      </main>
      <div class="p-20"></div>
      <Footer />
    </div>
  </body>
</html>
