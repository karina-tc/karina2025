---
import "../styles/global.css";
import type { HeaderProps } from "../types/HeaderProps";
import NavWrapper from "../components/header/NavWrapper.astro";
import Clouds from "../components/background/Clouds.astro";
import HeaderHero from "../components/structure/HeaderHero.astro";
import Footer from "../components/structure/Footer.astro";
import LoadingBar from "../components/elements/LoadingBar.astro";

interface Props extends HeaderProps {
  pageTitle: string;
  pageDescription: string;
  showBackToTop?: boolean;
}

const {
  pageTitle = "Karina Tovar",
  pageDescription = "Pixel Philosopher Cautious Technologist",
  showBackToTop = true,
  ...headerProps
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <meta name="description" content={pageDescription} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="w-full h-screen p-0 m-0 overflow-x-hidden relative">
    <LoadingBar />
    <Clouds loading="eager" />
    <NavWrapper />
    <HeaderHero {...headerProps}>
      <slot name="title-content" slot="title-content" />
    </HeaderHero>
    <main class="flex-1 relative">
      <slot />
    </main>
    <Footer showBackToTop={showBackToTop} />
  </body>
</html>

<script>
  import "../scripts/controllers/intersection-controller";
</script>
